name: Python Test job

on:
  push:
    branches: ["main"]
  pull_request:


jobs:
  test:
    runs-on: ubuntu-latest

    steps:
        - name: Checkout repo
          uses: actions/checkout@v4

        - name: Set up Python
          uses: actions/setup-python@v5
          with:
            python-version: "3.11"

        - name: Install dependencies
          run: |
            python -m pip install --upgrade pip
            echo "successful python -m pip install --upgrade pip"

        - name: Run Python file
          run: |
            python app.py

        - name: Install pytest
          run: pip install pytest

        - name: Run test
          run: pytest -v